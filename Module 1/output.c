#include <stdio.h>
int main()
{
    // int rahim, karim;
    // float f;
    // char c;
    // scanf("%d %f %c", &rahim, &f, &c);
    // printf("%d %0.1f %c", rahim,f, c);

    // for % Method-1:
    // int a, b;
    // char p;
    // scanf("%d%c %d%c", &a, &p, &b, &p);
    // printf("%d%% %d%%", a, b);

    // Method-2:
    int a;
    int b;

    scanf("%d%% %d%%", &a, &b);
    printf("%d%% %d%%", a, b);

    return 0;
}