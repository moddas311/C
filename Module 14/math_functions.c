// Ceil
/*
#include <stdio.h>
#include <math.h>
int main()
{
    double x;
    scanf("%lf", &x);
    int ans = ceil(x);
    printf("%d", ans);
    return 0;
}
*/

// Floor
/*
#include <stdio.h>
#include <math.h>
int main()
{
    double x;
    scanf("%lf", &x);
    int ans = floor(x);
    printf("%d", ans);
    return 0;
}
*/

// Round
/*
#include <stdio.h>
#include <math.h>
int main()
{
    double x;
    scanf("%lf", &x);
    int ans = round(x);
    printf("%d", ans);
    return 0;
}
*/

// Squrt
/*
#include <stdio.h>
#include <math.h>
int main()
{
    double x;
    scanf("%lf", &x);
    int ans = sqrt(x); // to get rootover value.
    printf("%d", ans);
    return 0;
}
*/

// Power
/*
#include <stdio.h>
#include <math.h>
int main()
{
    double x, y; // they can work both of type int ant float.
    scanf("%lf %lf", &x, &y);
    int ans = pow(x, y); // to get rootover value.
    printf("%lf", ans);
    return 0;
}
*/

#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main()
{
    int x;
    scanf("%d", &x);
    int ans = abs(x); // abs function er kaj holo negetive number k positive kora.
    printf("%d", ans);
    return 0;
}