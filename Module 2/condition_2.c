/*#include <stdio.h>
int main()
{
    int tk;
    scanf("%d", &tk);
    if (tk >= 100)
    {
        printf("Coke and halim.");
    }
    else if (tk >= 80)
    {
        printf("Mojo half litre and pori");
    }
    else if (tk >= 30)
    {
        printf("Ekta Icecreem.");
    }
    else if (tk >= 20)
    {
        printf("Ekta cha");
    }
    else
    {
        printf("Ajke kopale kichui nai.");
    }
    return 0;
}

#include <stdio.h>
int main()
{
    int junaid;
    scanf("%d", &junaid);
    if (junaid >= 500 && junaid <= 1000)
    {
        printf("He will buy Grils, dhoi and chotpoti.");
    }
    else if (junaid >= 300 && junaid <= 499)
    {
        printf("He will buy halim, chotpoti and coke");
    }

    else
    {
        printf("Goribs.");
    }
    return 0;
}
*/

#include <stdio.h>
int main()
{
    int abc;
    scanf("%d", &abc);

    if (abc >= 1000 || abc >= 2000)
    {
        printf("He is rich kid.");
    }

    else if (abc >= 500 || abc >= 700)
    {
        printf("He has money but not to much.");
    }

    else if (abc >= 100 || abc >= 150)
    {
        printf("He has not enough money");
    }

    else

    {
        printf("Very poor guy.");
    }
    return 0;
}