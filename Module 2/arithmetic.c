#include <stdio.h>
int main()
{
    // int a = 10, b = 20;

    // for (+)
    // int sum = a + b;

    // for (-)
    // int sum = b - a;

    // for (*)
    // int multiply = a * b;

    // for (/)
    // int division = b / a;

    // for float value

    int x = 20, y = 3;
    // float division = x* 1.0 / y;

    // for (%)

    int mod = x % y;
    printf("%d", mod);
    return 0;
}