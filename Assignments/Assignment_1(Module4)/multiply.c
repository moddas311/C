// Question_2: You will be given two integers A and B. You need to give output their multiplication.

#include <stdio.h>
int main()
{
    long long int a, b, multipication;
    scanf("%lld %lld", &a, &b);
    multipication = a * b;
    printf("%lld", multipication);
    return 0;
}