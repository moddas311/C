// Problem_2: Take two integers A and B as input and output their summation.
#include <stdio.h>
int main()
{
    int a, b, sum;
    scanf("%d %d", &a, &b);
    sum = a + b;
    printf("%d", sum);
    return 0;
}
